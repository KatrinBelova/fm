import React from "react";
import { CircularProgress, Box } from "@material-ui/core";

const Spinner = () => {
  return (
    <Box m={"auto"}>
      <CircularProgress />
    </Box>
  );
};

export default Spinner;
